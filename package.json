{
  "name": "dns-leak-test",
  "version": "2.0.0",
  "description": "Self-hosted DNS leak test - hardened, authoritative-only DNS, lokalni geo lookup",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "NODE_ENV=development node --watch src/index.js"
  },
  "dependencies": {
    "dns2": "^2.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "maxmind": "^4.3.18"
  },
  "engines": {
    "node": ">=18"
  }
}
